{
  "name": "stripe-reconciliation",
  "version": "1.0.0",
  "description": "Stripe Payment Reconciliation Tool with Vercel API",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  }
}
